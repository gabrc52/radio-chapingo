import 'package:flutter/material.dart';

class RadioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'No se pudo encontrar la programación',
        style: Theme.of(context).textTheme.headline6,
      ),
    );
  }
}
